import 'package:flutter/material.dart';

class Exercice2Screen extends StatelessWidget {
  const Exercice2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Exercice 2")),
      body: const Center(child: Text("Contenu de l'exercice 2")),
    );
  }
}